# 如何启动这个项目（新手版）

## 一、先理解：这个项目要同时跑 3 个东西

| 服务 | 端口 | 干什么的 | 什么时候需要它 |
| --- | --- | --- | --- |
| **Redis** | 6380 | 存 FAQ 问答数据（地址/电话/营业时间） | 一直跑着就行，基本不用管 |
| **后端** | 8000 | FastAPI 接口 + AI 大脑（调 DeepSeek） | 每次要用 AI 对话时都要开 |
| **前端** | 3000 | 网页界面，你实际看到的点餐页面 | 每次要看界面时都要开 |

> 三个缺一不可：只开前端 → 页面出来但没数据；只开后端 → 没人给你界面。

---

## 二、方式 A：在 VS Code 里启动（推荐，日常开发用这个）

### 第 1 步：启动前端

> ⚠️ **先做这一步**：如果 VS Code 底部已经开着终端（尤其是报错过的那个），**点终端面板右上角的垃圾桶图标把它关掉**。原因见下方【八、npm 报错】。

1. 按 `Ctrl + Shift + P`
2. 输入 `Run Task`，选 **`Tasks: Run Task`**
3. 在列表里点 **`① 启动前端 (Vite)`**

下面会弹出一个终端，出现这样的内容就是成功了：

```
VITE v4.5.14  ready in 576 ms

➜  Local:   http://localhost:3000/
```

> ⚠️ 这个终端**不要关**，前端就靠它活着。

### 第 2 步：启动后端（带断点调试）

1. 按 `Ctrl + Shift + D` 打开「运行和调试」面板
2. 顶部下拉框选 **`★ 启动后端（带调试，自动准备 Redis/FAQ）`**
3. 按 `F5`（或点绿色三角 ▶）

终端里出现这两行就是成功了：

```
Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

> 这个配置会**自动帮你检查并启动 Redis**，还会把 FAQ 数据同步进去，你不用手动管 Redis。

### 第 3 步：打开网页

浏览器访问 👉 **http://localhost:3000**

---

## 三、方式 B：双击 start.bat（最省事，一键全开）

直接在文件管理器里双击项目根目录的 **`start.bat`**。

它会自动：检查 Redis → 同步 FAQ → 开后端窗口 → 开前端窗口 → 打开浏览器。

- 会弹出两个黑窗口：`API-8000` 和 `UI-3000`
- **停止服务 = 关掉这两个黑窗口**
- 在 VS Code 里也能用：`Ctrl+Shift+P` → `Tasks: Run Task` → `一键启动全部 (调用 start.bat)`

---

## 四、怎么停止服务

| 服务 | 停止方法 |
| --- | --- |
| 前端 | 在它所在的终端里按 `Ctrl + C` |
| 后端（F5 起的） | 按 `Shift + F5`，或点调试工具条的红色方块 ■ |
| 后端（脚本起的） | 关掉 `API-8000` 黑窗口 |
| Redis | 一般不用停。要停就关掉 `Redis-6380` 窗口 |

---

## 五、常见报错对照表

| 报错信息 | 什么意思 | 怎么解决 |
| --- | --- | --- |
| `OSError: [WinError 10048] 通常每个套接字地址...只允许使用一次` | **端口被占了**，已经有同一个服务在跑 | 说明后端/前端已经启动过一次。先把它停掉，再重新 F5 |
| `ERR_ADDRESS_INVALID (-108)` | 你访问的是 `0.0.0.0:8000` | `0.0.0.0` 不能当网址。改用 `http://127.0.0.1:8000/docs` |
| `{"detail":"Not Found"}` | 访问了根路径 `/` | 正常现象。后端只注册了 `/chat`、`/menu/list`、`/faq/suggest`、`/reservation/list`。想看接口列表用 `/docs` |
| 前端地址变成 3001 | 已经有一个前端在 3000 跑了 | 先停掉旧的那个（Ctrl+C），再重开 |
| `Missing credentials` / 401 | DeepSeek 的 Key 没配好 | 检查 `.env` 里的 `OPENAI_API_KEY` |
| 数据库连不上 / 表不存在 | 连错 MySQL 实例了 | 本项目 MySQL 在 **3307**。`3306` 是禅道自带的，别动它 |
| `无法加载文件 D:\npm.ps1，因为在此系统上禁止运行脚本` | PowerShell 执行策略限制 | **已修复**，详见下一节 |

---

## 六、重要：端口占用说明（本机特殊情况）

这台电脑上装了**禅道（ZenTao）**，它占用了几个常用默认端口：

| 默认端口 | 实际被谁占了 | 本项目用什么 |
| --- | --- | --- |
| 3306 | 禅道自带 MySQL | 官方 MySQL 8.0 → **3307** |
| 6379 | 禅道自带 Redis（要密码） | 另起的 Redis → **6380** |

**禅道的服务不要去动**，动了禅道会起不来。

---

## 七、想改代码的话

| 我想改… | 改这个文件 |
| --- | --- |
| 接口（加接口、改返回） | `api/main.py` |
| AI 的工具、模型、提示词 | `agent/langchain_assitant.py` |
| AI 的人设 | `agent/prompts/system_prompt.txt` |
| FAQ 问答内容 | `agent/redis_data_sync.py`（改完要重新同步） |
| 菜品数据 | 数据库里的 `menu.menu_items` 表 |
| 页面 | `ui/src/App.vue` |

> 改后端代码时，推荐用调试配置 **`后端 - 改代码自动重启（开发推荐）`**，保存文件后它会自己重启，不用手动停再开。

---

## 八、npm 报 `无法加载文件 D:\npm.ps1`（已修复，了解一下）

### 当时报的错

```
npm : 无法加载文件 D:\npm.ps1，因为在此系统上禁止运行脚本。
    + CategoryInfo   : SecurityError: (:) [], PSSecurityException
    + FullyQualifiedErrorId : UnauthorizedAccess
```

### 什么原因

不是你的代码问题，也不是 npm 坏了，是 **Windows 的 PowerShell 默认不允许运行 `.ps1` 脚本**。

链条是这样的：

1. 你的 node 装在 **D 盘**（`D:\node.exe`），所以 npm 也有三份"分身"：

   | 文件 | 给谁用 |
   | --- | --- |
   | `D:\npm.cmd` | 命令行（cmd）用 |
   | `D:\npm.ps1` | **PowerShell 用** ← 问题在这 |
   | `D:\npm` | Git Bash 用 |

2. VS Code 的终端**默认就是 PowerShell**，而 PowerShell 优先挑 `.ps1` 那份来执行。
3. Windows 默认的执行策略是 `Restricted`（禁止运行任何脚本文件），于是 PowerShell 拒绝执行 `npm.ps1` → 报错。

### 已经修好了，做了两件事

**① 改了执行策略（只改"当前用户"，不动系统）**

```
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

`RemoteSigned` 是微软推荐值：本机的脚本可以跑，从网上下载的脚本必须签名才能跑。

- 只影响当前登录用户，**没有动系统级设置**
- 想撤销随时可以：`Set-ExecutionPolicy -Scope CurrentUser Undefined`

**② 让 VS Code 任务改用 `npm.cmd`**

`.vscode/tasks.json` 里两个 npm 任务已从 `npm` 改成 **`npm.cmd`**：

| 任务 | 命令 |
| --- | --- |
| ① 启动前端 (Vite) | `npm.cmd run dev` |
| 安装/更新前端依赖 | `npm.cmd install` |

`.cmd` 由 cmd 解释，**完全不受 PowerShell 执行策略影响** —— 就算以后策略又被改回去，这两条任务照样能跑。属于双保险。

### 你要做的一件事

**把 VS Code 里已经开着的终端关掉再重开**（点终端面板的垃圾桶图标 🗑）。

> 原因：PowerShell 在**进程启动那一刻**就把执行策略读进内存了。已经开着的旧终端还是"旧规矩"，必须新开一个才认新策略。

关掉之后重新 `Run Task` → `① 启动前端 (Vite)` 即可。

### 以后在终端里手敲 npm 也行吗？

**行。** 策略改完后，新开的终端里直接敲 `npm run dev` 也正常 —— 这也是为什么建议顺手把策略改掉，而不只是改任务配置。

### 万一还是不行

用这种方式绕过（把命令写全，PowerShell 会直接用 `.cmd`）：

```powershell
cd C:\Users\ymq\Desktop\SmartOrderingAgent\ui
npm.cmd run dev
```

再不行就把 VS Code 的默认终端换成 cmd：
`Ctrl+Shift+P` → `Terminal: Select Default Profile` → 选 **`Command Prompt`** → 关掉终端重开。
